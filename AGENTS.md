# Agent Rules

The canonical agent guide for this repo is **[`CLAUDE.md`](CLAUDE.md)** — commands,
architecture, secrets, default programs, and the feature-test model all live there.
Read it first. Domain vocabulary is in [`CONTEXT.md`](CONTEXT.md); agent-tooling
config (issue tracker, triage labels, domain-doc rules) is under `docs/agents/`.
