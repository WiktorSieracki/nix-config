# Secrets
*.secret
*.key
secrets/

# Build outputs
result
result-*
# Compiled Go binary of the switchboard feature (built by buildGoModule from src)
/modules/features/switchboard/switchboard

# VM / disk images (built by `build.vm` runner, build-image, or live-ISO QEMU runs)
*.qcow2
*.iso
*.img

# Python bytecode (e.g. py_compile checks of feature scripts)
__pycache__/

# AI tooling scratch
.claude-flow/